# Sistem / editör
.DS_Store
.claude/

# .NET derleme çıktıları
bin/
obj/
*.user
TestResults/

# Hassas yapılandırma (ENV.md — gizli değerler kaynak koda/commit'e girmez)
appsettings.Development.json
**/Properties/launchSettings.json

# Yerel geliştirme/test hesap bilgileri (gerçek şifre içerir, commit'e ASLA girmez)
DEV_TEST_ACCOUNT.local.md

# Loglar (Serilog dosya sink — logs/app-*.txt)
logs/

# Yüklenen görseller (LocalFileStorageService, A-08 — geliştirmede diske yazılan
# runtime çıktısı, kaynak koda ait değil, .gitkeep dışında commit edilmez)
backend/WordLearner.API/wwwroot/uploads/*
!backend/WordLearner.API/wwwroot/uploads/.gitkeep
